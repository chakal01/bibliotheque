= bootstrap_form_for @genre, layout: :horizontal do |f|
  - if @genre.errors.any?
    #error_explanation
      %h2= "#{pluralize(@genre.errors.count, "error")} prohibited this genre from being saved:"
      %ul
        - @genre.errors.full_messages.each do |msg|
          %li= msg

  = f.text_field :nom
  = f.submit 'Save', class: "btn"
